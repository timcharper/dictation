# TODO

- [ ] Handle "no microphone" source case in the daemon. Show an error notification via the GNOME extension or a system sound/visual cue if no input device is available.
- [ ] Implement dynamic settings UI for backend selection (WhisperCpp vs OpenAI).
- [ ] Add support for other LLM backends (OpenAI, Anthropic) in addition to Ollama.
- [ ] Improve VAD sensitivity settings in the UI.
